<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.jsoftti.pvfabrica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PrincipalActivity" />
        <activity android:name=".activities.ProdutosActivity" />
        <activity android:name=".activities.CadastrarProdutoActivity" />
        <activity android:name=".activities.ClienteActivity" />
        <activity android:name=".activities.ClientesActivity"></activity>
    </application>

</manifest>